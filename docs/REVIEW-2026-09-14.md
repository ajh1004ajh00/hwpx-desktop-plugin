# 2026-09-14 자동 검증 기록

기준 main: `9427866d4bce479e70a5baaa9ba56d9cdebbd056`.
작업 위치: `C:\Projects\hwpx-desktop-plugin-review`, `fix/25-review-readiness`의 미커밋 변경본.
원래 main과 설치된 플러그인 캐시는 변경하지 않았다. 커밋·push·PR·배포·심사 제출은 수행하지 않았다.

## 적용한 변경

- #19·#20: 사용자 승인 정책을 문서화하고 모델 응답에서 파일명, 주변 문맥, 내부 검증 해시/토큰과 쓰기 원문을 제외했다. 내부 상태 검증과 동일 문서/Undo 경로는 유지했다. 표/문서 분석은 별도 요청 범위다.
- #25: 부분 등록 중/실패 후/정리 후 콜백 실행을 거부한다. 해제 메서드가 있는 host에서는 그 메서드를, 없는 host에서는 AbortSignal을 제공한다. 둘 다 무시하는 host의 목록 제거를 보장하지 않지만 남은 콜백은 비활성화된다. 정리 오류가 최초 등록 오류를 덮지 않는다. 페이지 이동·복원 연결을 추가했다.
- #13: Windows CI 정의(아직 원격 실행 안 함), 로컬 빌드 파일 목록·버전·SHA-256 검사, 누락/변경/문서 혼입 실패 테스트를 추가했다. upstream public/samples 예제 문서는 빌드에서 제외했다. 실제 사용자 문서의 유출을 발견했다는 뜻이 아니다.
- #1·#14·#16: [사람 검증 인계서](REVIEW-HANDOFF.md)와 [데이터 처리 기준](DATA-POLICY.md)을 작성했다. 정상 7개·부정 7개 시나리오는 준비된 절차이며 사람이 실행한 결과가 아니다.

## 이번 실행 결과

| 검사 | 관찰 결과 |
|---|---|
| 격리 worktree에서 npm run setup | 성공. 고정 upstream·lockfile 설치 및 빌드 |
| 수정 전 npm test | 18/18 통과 |
| 새 보안/생명주기 회귀 검사 최초 실행 | 7개 실패로 기존 문제 재현 |
| 수정 후 npm test | 28/28 통과 |
| npm run build | 성공. 고정 변환 marker 검사 포함 |
| 패키지 실패 주입 | 누락 파일·버전 혼합·바이트 변경·문서 혼입을 거부 |
| node --check desktop/studio.mjs | 통과 |
| git diff --check | 통과 |
| npm audit --omit=dev --prefix desktop | 0건 |
| npm audit --omit=dev --prefix desktop/.runtime/rhwp/rhwp-studio | 0건 |
| upstream 빌드 의존성 포함 audit | 5건: low 1, moderate 1, high 3. @babel/core, baseline-browser-mapping, brace-expansion, browserslist, fast-uri. 미조치 |

빌드에서 CanvasKit의 fs/path 외부화와 큰 chunk 경고가 남는다. 이 결과는 실제 Desktop/IME/파일 내보내기 시험을 대신하지 않는다. 빌드 도구 advisory는 고정 upstream과의 호환성을 검토한 뒤 재현 가능한 lockfile 수정으로 해결해야 한다. 자동 audit fix로 upstream 고정을 바꾸지 않았다.

패키지 검사 사용법:

```powershell
node scripts/verify-package.mjs > test-results/package-inventory.json
node scripts/verify-package.mjs --against test-results/package-inventory.json
```

첫 명령은 신뢰한 로컬 빌드의 목록을 만들고 둘째는 같은 목록과 바이트를 비교한다. 서명 또는 공급자 진위 인증이 아니다. 최종 제출 아카이브 구조·완전한 비밀값 탐지·의존성별 법적 고지 검토는 아직 완료되지 않았다.

## 남은 작업과 판정

실제 Desktop 도구 표시·AbortSignal/주석 효과, UI 이동/복귀, 사람의 직접 입력과 AI Undo, Windows 형식별 저장·재열기, 최종 새 사용자 설치는 미검증이다. macOS는 사용자 지시로 실행하지 않았다. GitHub Actions 정의는 로컬 파일이며 원격 CI 통과를 주장하지 않는다.

26개 이슈 전체를 완료한 것은 아니다. #1의 제출 환경 확인 후 #12·#22·#23·#24의 최종 배포 경로를 정해야 한다. #2 첨부 연결, #4 자연어 탐색, #6 여러 항목 작성, #7 변경 확인 UX, #8 복구 UX, #9 호환성 문서, #21 실제 통신/저장 검증, #26 분석 성능/응답 한도 및 형식별 검증은 계속 남아 있다. 기존 기능에 이미 구현된 부분과 사람 확인이 필요한 부분을 구분해 후속 진행한다.

따라서 관련 이슈는 닫지 않으며 심사 제출 준비 완료로 표시하지 않는다.
